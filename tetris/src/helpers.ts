import { CELL_SIZE } from "./constants";
// itemsArray
//     .map((rowItems, rowIdx) =>
//         rowItems.map((_, colIdx) => {
//             const cell = new Graphics();
//             const x = xOffset + colIdx * CELL_SIZE.width;
//             const y = yOffset + rowIdx * CELL_SIZE.height;

//             cell.rect(x, y, width, height).stroke({ width: 2, color: colors.mainWeak });

//             return cell;
//         }))
//     .flat();